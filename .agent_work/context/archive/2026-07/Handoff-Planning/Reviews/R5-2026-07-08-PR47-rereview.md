# TowerScout External Review — R5 (PR #47 re-review after R4 fixes)

> **HISTORICAL REVIEW**: Preserve as evidence. Current pilot/adoption action is
> defined in `../PILOT-FEEDBACK-AND-CDC-AI-ADOPTION-PLAN.md`.

| | |
|---|---|
| **Review round** | R5 (follows R4, same day; next round will be R6) |
| **Date** | 2026-07-08 |
| **Reviewed state** | PR #47 @ `5fe2f46` (draft), base `main` @ `dfe9e6b` (unchanged — no drift, freeze holding), 4 commits, `mergeable_state: clean`, both workflows green at the head |
| **Scope** | Verify every R4 finding (B1, B2, S1, S2, S3) against the new head; final merge-readiness verdict |

---

## 1 · Verdict

**Merge-ready, with one small pre-merge action and one merge-method instruction.**

All five R4 findings are correctly resolved — verified in the diff, not the description. The PR is now 11 files, +1,122/−13: the launcher lib, the status script, two test files, `.gitignore`, and six `.agent_work`/planning record files. CI is green on both workflows at `5fe2f46`.

Remaining before undrafting:

1. **Record the post-fix Windows test run** (R4 checklist item 4). The fix commit's message is bare and TASK-088 was not updated with it, so there is no record that the Windows-only behavioral slices ran *after* the B2/S1/S2/S3 changes — and CI cannot run them (they skip on Linux). One PR comment or TASK-088 log line with the four slice results plus the new down-state test is sufficient: `test_task_074_bootstrap`, `test_task_081_runtime_hardening`, `test_import_assets_script`, `test_release_package_script`.
2. **Squash-merge this PR** (supersedes R4 §5's "merge normally"). The debris was added in `44b94db` and removed in `5fe2f46`, so the ~25,000 junk lines **including a binary ZIP still exist as blobs in the branch history**. A regular merge carries those blobs into `main`'s permanent object history — right before the cdcai push. The branch is linear with no upstream merge inside (verified), so squashing is safe here and keeps `main`'s history clean with the net 11-file change. This is the same fork-internal squash precedent as PR #46; the never-squash rule applies only to the cdcai-merge-bearing history and pushes into cdcai.

---

## 2 · R4 findings — verification against `5fe2f46`

| ID | Claimed fix | Verified | Evidence |
|---|---|---|---|
| **B1** debris | Removed + ignored | **CONFIRMED** | `5fe2f46` deletes all 15 debris files (25,072 deletions, including the binary ZIP); no `artifacts/` or `test-artifacts/` paths remain in the PR diff; `.gitignore` gains `test-artifacts/` (line 19) and `artifacts/` (line 41) |
| **B2** stopped-app false mismatch | Reason-gated | **CONFIRMED** | `status.ps1`: the mismatch warning + `exit 1` now requires `Reason -eq "mismatch"`; `container_not_found` prints "No running TowerScout container found for this package." and falls through to the readiness probe (down state again reports "readiness endpoint is not reachable", exit 2). New behavioral test `test_status_script_preserves_down_state_when_container_is_missing` stubs the check to `container_not_found`, runs the real script, and asserts exit code 2, the informational message present, and the mismatch warning **absent** — exactly the pin R4 requested |
| **S1** status env sync | Added | **CONFIRMED** | `status.ps1` now calls `Initialize-TowerScoutEnvFile -RootPath $repoRoot` immediately after dot-sourcing the lib, before any engine work; source-contract test asserts the call. (`stop.ps1`/`logs.ps1` were left unmodified — that part of S1 was "consider", and the disposition is acceptable; noted as an optional post-release follow-up) |
| **S2** `$matches` collision | Renamed | **CONFIRMED** | `Test-TowerScoutRunningImageMatchesPackage` now uses `$isMatch` throughout (lib lines 1220–1244); the function returns a real boolean. The one remaining `$matches[1]` (line 158) is pre-existing, legitimate regex-capture usage in a different function immediately after its own `-match` — correct as-is |
| **S3** NVIDIA overlay vars | Added | **CONFIRMED** | `NVIDIA_DRIVER_CAPABILITIES` and `NVIDIA_VISIBLE_DEVICES` added to the managed set (lib lines 461–462); source-contract test asserts both names |

Also re-verified at the new head: the final lib delta between R4's head and this one is *only* the NVIDIA pair + the rename (12 lines) — nothing else changed in the launcher; the status delta is only the reason-gating + sync + message (8 lines); test additions are +50 lines in `test_task_081` with zero modifications to existing assertions.

---

## 3 · After the merge

- Freeze `main` once the squash lands and CI is green at the squash SHA.
- Proceed directly to the R3 §5.3 `v0.1.1` flow: tag the green tip, dispatch both builds from ref `v0.1.1` (`push_latest=false`), record the new digests (must differ from `0c1ea503…`/`9f1563eb…`), rebuild packages, run the §5.5 D4 logistics.
- Minor reminder: the two `image-metadata-v0.1.0-*.json` files deleted with the debris are useful build-provenance evidence — keep copies with the validation-evidence archive (they remain available as workflow-run artifact downloads for 90 days).
- R6 will verify: squash SHA green; `v0.1.1` tag/builds/packages per R3 §6 and R4 §6, including `status.cmd` behavior on a running cell (digest line printed, exit 0) and a stopped cell (down-state message, no mismatch warning, exit 2).

*Prepared by the external reviewer, 2026-07-08. All verification anchors are at PR head `5fe2f46`: `.gitignore:19,41`; `status.ps1:12,33-45`; TowerScoutCompose.ps1:458-462 (managed set), 1217-1244 (`$isMatch`); `test_task_081_runtime_hardening.py` (down-state + contract assertions).*
