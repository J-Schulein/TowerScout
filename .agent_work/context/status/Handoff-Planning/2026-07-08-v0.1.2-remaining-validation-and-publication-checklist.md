# TowerScout v0.1.2 Validation And Publication Checklist

> **CURRENT CLOSEOUT NOTE**: Validation/publication are complete. `v0.1.2` is
> now the frozen fork-side pilot baseline. Keep `cdcai/TowerScout` unchanged
> until pilot feedback is reviewed and the owner approves adoption. See
> `PILOT-FEEDBACK-AND-CDC-AI-ADOPTION-PLAN.md`.
> Pilot distribution and Task-088 closeout were completed by 2026-07-22.

**Date**: 2026-07-08
**Purpose**: Track completed D4/full-matrix validation and GitHub Release promotion work for the active stable release line
**Frozen baseline already used for tag/build**: `origin/main@718a564`
**Active release identity**: `v0.1.2`

## What Is Already Complete

- `v0.1.2` tag created and pushed from `718a56485a59182f060a537e8f11d4ce71a1f0d4`
- GHCR images published:
  - CPU: `sha256:86c54bd723ff970f70f0883397a1f2f804db796507a461a5718aeab57258afe8`
  - CUDA: `sha256:bab2eda26fa6cf0483780cfcdb0a10008fb67fe058ba99a28ebdd6212fda2214`
- Shared asset ZIP staged as:
  - `towerscout-v0.1.2-assets-towerscout-v1-assets-2026-05-05.zip`
- CPU and CUDA control packages rebuilt from the clean `v0.1.2` worktree
- Package integrity checks passed:
  - ZIP sidecars
  - package summaries
  - manifest validation
- Pre-tag local packaged Docker CPU smoke from the frozen merge candidate passed
- Final package runtime smokes passed on:
  - Docker CPU package path
  - Podman CPU package path
  - Docker CUDA package path with `-Gpu off`
- Full D4/full-matrix validation passed on 2026-07-09 against the exact
  `Validation v0.1.2-1` prerelease assets:
  - Docker CPU
  - Docker CUDA
  - Podman CPU
  - Podman CUDA
  - Google and Azure provider paths
  - fixture parity, live smokes, exports, wizard S1-S7, identity gates, and
    stopped-status contracts
- The existing validation prerelease was promoted in place on 2026-07-09:
  - title `TowerScout v0.1.2`
  - `isPrerelease=false`
  - listed as `Latest`
  - URL `https://github.com/J-Schulein/TowerScout/releases/tag/v0.1.2`

## Closeout State

1. Sanitized full-matrix evidence is preserved with the repository record.
2. The pilot email was sent on 2026-07-13 and distinguishes the fork download
   from the unchanged official cdcai repository.
3. Feedback is maintained by the project lead in an external fillable Word
   document; primary and backup support contacts have appropriate access.
4. Release assets, checksums, validation summary, known findings, and
   troubleshooting material have durable public/repository custody.
5. Task-089 remains preparation-only until feedback is reviewed and the cdcai
   owner approves an adoption baseline.

## Inputs To Carry Forward

### Published image digests

- CPU image:
  - `ghcr.io/j-schulein/towerscout:v0.1.2-cpu@sha256:86c54bd723ff970f70f0883397a1f2f804db796507a461a5718aeab57258afe8`
- CUDA image:
  - `ghcr.io/j-schulein/towerscout:v0.1.2-cuda121@sha256:bab2eda26fa6cf0483780cfcdb0a10008fb67fe058ba99a28ebdd6212fda2214`

### Shared asset ZIP checksum

- `00599cc4fe9f2bdb4708c669d7c3d9a8a570a0c3b547bc5c317026196c7bacbb`

### Runtime checks already validated

- Docker CPU package:
  - port `5027`
  - app `v0.1.2`
  - `asset_status=ok`
  - running image digest matches CPU digest
- Podman CPU package:
  - port `5028`
  - app `v0.1.2`
  - `asset_status=ok`
  - running image digest matches CPU digest
- Docker CUDA package with `-Gpu off`:
  - port `5029`
  - app `v0.1.2`
  - `asset_status=ok`
  - `pytorch_flavor=cuda121`
  - `selected_device=cpu`
  - running image digest matches CUDA digest

## Completed D4 Setup Requirements

- archived `ts-detect-harness.ps1` reused as a version-agnostic harness
- archived `fixtures-20260707` copied and re-hashed
- 12 fixture tile SHA256 prefixes verified
- `-CaptureFixtures` was not used
- packages were extracted to space-free paths
- cells ran sequentially on port 5000
- validation keys were staged locally, not printed, and deleted at run end

## D4 Evidence Captured

For each cell, the evidence captures readiness identity, engine-level image
identity from status output, and independent engine inspect checks. The printed
running digest matched the pinned package digest for every cell.

## D4 CPU Cell Result

Docker CPU passed package setup, readiness, identity gates, two fixture passes,
wizard S1-S7, live smokes, Google/Azure exports, and stopped-status validation.
Podman CPU passed package setup, readiness, identity gates, fixture parity,
live smokes, and Google/Azure exports.

## D4 GPU Cell Result

Docker CUDA and Podman CUDA passed package setup, readiness, identity gates,
`selected_device=cuda` on the NVIDIA T1000 host, fixture parity, live smokes,
and Google/Azure exports. Podman CUDA also validated GPU/CDI enablement from a
factory-reset Podman machine.

## D4 Pass Criteria

- [x] engine-level digest cross-check matched the pinned digest on each cell
- [x] readiness matched the expected app version and engine/device state
- [x] fixture totals remained `53` per cell
- [x] Docker CPU pass1 == Docker CPU pass2 on all 12 tiles
- [x] CPU == GPU and Docker == Podman on all 12 tiles
- [x] live smoke succeeded per protocol
- [x] Setup-Wizard manual smoke S1-S7 passed

## Release Publication Assets

Expected `v0.1.2` GitHub Release assets:

1. `towerscout-v0.1.2-cpu.zip`
2. `towerscout-v0.1.2-cpu.zip.sha256`
3. `towerscout-v0.1.2-cuda121.zip`
4. `towerscout-v0.1.2-cuda121.zip.sha256`
5. `towerscout-v0.1.2-assets-towerscout-v1-assets-2026-05-05.zip`
6. `towerscout-v0.1.2-assets-towerscout-v1-assets-2026-05-05.zip.sha256`

## Release Publication Command

Completed action: promoted the existing validation prerelease in place instead
of creating a second release record for the same tag/assets.

```bash
gh release edit v0.1.2 \
  --repo J-Schulein/TowerScout \
  --latest \
  --title "TowerScout v0.1.2" \
  --notes-file .agent_work/context/status/Handoff-Planning/2026-07-08-v0.1.2-release-notes-draft.md \
  --prerelease=false
```

## Release Notes Requirements

The `v0.1.2` notes should explicitly state:

- `v0.1.0` was tagged and images published, but no final release assets shipped
- `v0.1.1` was also tagged and images published, but no final release assets shipped because the Docker image-identity helper needed a follow-up fix
- `v0.1.2` is the first stable release in this line whose source, images, packages, and package-validation behavior all align

## Post-Publication Closeout

After publication:

1. [x] record final digests, checksums, release URL, and validation evidence in TASK-088
2. [x] update TASK-089 with the final release URL while preserving `v0.1.2` as the cdcai migration baseline and `v0.1.0` / `v0.1.1` as historical fork-only tags
3. [x] preserve sanitized D4 evidence with the repository/release record
4. [x] record the external Word feedback method and confirmed primary/backup
       support coverage without publishing contact details
5. [x] verify the pilot guide points to the fork-side `v0.1.2` assets and does
       not present cdcai as the pilot download
6. [x] keep `cdcai/TowerScout` unchanged during the feedback hold

## Stop Conditions

No stop condition was hit during validation. Re-evaluate before publication
only if the release assets or notes are changed after this evidence set:

- D4 fixture totals drift from `53`
- CPU pass1 != CPU pass2 on any tile
- CPU != GPU on any tile
- running image digest does not match the pinned package digest on any cell
- Setup-Wizard manual smoke S1-S7 fails
- live smoke fails after the validation `.env` is confirmed to carry the post-rotation Azure key
